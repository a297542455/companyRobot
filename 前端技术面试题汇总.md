# 前端技术面试题汇总

## 一、英文面试题

### 1\. good afternoon， could you introduce yourself briefly

> 自行准备英文自我介绍
> 
> 

### 2\. what is your greatest strengths

> 自行作答
> 
> 

### 3\. what is your biggest weakness

> 自行作答
> 
> 

---

## 二、CSS 相关

### 1\. CSS 垂直居中、Grid 布局

> 自行补充作答
> 
> 

### 2\. 单行 / 多行文本溢出省略样式

#### 单行文本溢出省略

**原理说明**

- `white\-space`：设置文本强制单行显示，禁止换行

- `overflow`：隐藏超出容器的文本内容

- `text\-overflow`：文本溢出时展示省略符号

```css
white-space: nowrap;
overflow: hidden;
text-overflow: ellipsis;
```

#### 多行文本溢出省略（WebKit 内核通用方案）

```css
overflow: hidden;
display: -webkit-box;
-webkit-line-clamp: 3;        /* 限制展示行数，可自定义 */
-webkit-box-orient: vertical;  /* 设置盒子垂直排列 */
```

---

## 三、DOM 操作

### 常见 DOM 操作方法

1. **元素获取**

    - `document\.querySelector`：根据选择器获取单个元素

    - `document\.querySelectorAll`：根据选择器获取元素集合

2. **节点创建与新增**

    - `document\.createElement`：创建元素节点

    - `appendChild`：向父节点尾部追加子节点

3. **节点删除**

    - `removeChild`：删除指定子节点

4. **内容与属性操作**

    - `innerHTML` / `innerText`：读写元素内容

    - `setAttribute` / `getAttribute`：设置、获取标签原生属性

5. **类名操作**

    - `classList\.add` / `classList\.remove`：添加、移除 CSS 类名

6. **节点关系**

    - `parentNode`：获取父节点

    - `children`：获取所有子元素节点

7. **事件绑定**

    - `addEventListener`：绑定事件监听

8. **元素尺寸**

    - `clientWidth` / `clientHeight`：获取元素可视宽高（包含 padding，不含 border）

---

## 四、构建工具

### 1\. Vite 和 Webpack 的区别

> 自行补充作答
> 
> 

### 2\. Tree Shaking（摇树优化）

1. **底层原理**
基于 ES6 `import` / `export` 模块化语法，ES6 模块为**静态模块**，编译阶段即可确定模块依赖关系。

2. **核心作用**
编译时扫描代码，识别**未被引用、未使用**的模块 / 变量 / 函数，删除冗余代码，缩减最终打包体积。

---

## 五、JavaScript 基础

### 1\. JS 浮点数精度问题

1. **产生原因**
JS 使用 64 位双精度浮点数存储数值；部分十进制小数转为二进制时会形成无限循环小数，受存储位数限制产生舍入误差，转回十进制后出现计算偏差。

2. **解决方案**

    - 使用 `toPrecision\(\)` / `toFixed\(\)` 做精度截取

    - 引入专业库：`Math\.js`、`BigDecimal\.js`

### 2\. this 指针指向判断规则

1. 若为**箭头函数**：无自身 `this`，直接继承外层作用域的 `this`；

2. 普通函数，根据**调用方式**判断：

    - `xxx\.fn\(\)`：`this` 指向调用对象 `xxx`

    - 独立调用 `fn\(\)`：严格模式下 `this = undefined`，非严格模式指向全局对象；

3. 被 `call` / `apply` / `bind` 绑定：`this` 指向手动绑定的目标。

### 3\. 大文件上传 \&amp; WebWorker 优化

> 自行补充作答
> 
> 

---

## 六、React 相关

### 1\. 获取子组件实例、读写属性 / 调用方法

#### 类组件

使用 `React\.createRef\(\)` 创建 ref 并绑定到子组件，通过 `ref\.current` 直接获取子组件实例，可访问实例属性、调用实例方法。

#### 函数组件

函数组件无真实实例，需组合 API 实现：

1. `forwardRef`：让函数组件支持接收外部传入的 ref；

2. `useImperativeHandle`：手动对外暴露需要访问的属性、方法；

3. 父组件通过 `ref\.current` 获取子组件暴露的对象，完成读写、方法调用。

### 2\. 提升组件渲染效率，避免不必要的重渲染

1. `memo`：包装函数组件，对 `props` 做浅对比，属性无变化则不重渲染；

2. `useCallback`：缓存事件函数，保持函数引用不变，配合 `memo` 生效；

3. `useMemo`：缓存对象、数组、计算结果，避免引用类型数据频繁变更；

4. **组件拆分**：将状态抽离到最小单元组件，缩小渲染范围；

5. **规范 key**：列表渲染使用稳定唯一的 key，减少 DOM Diff 开销；

6. 类组件优化：

    - `PureComponent`：自动对 `props`、`state` 做浅对比；

    - `shouldComponentUpdate`：手动编写判断逻辑，精准控制是否重渲染。

---

## 七、网络相关

### 1\. 常见 HTTP 状态码

- **2xx 请求成功**

    - 200 OK：请求成功，正常返回数据

    - 201 Created：资源创建成功（多用于 POST / PUT）

- **3xx 重定向 \&amp; 缓存**

    - 302 Found：临时重定向

    - 304 Not Modified：资源未更新，使用本地协商缓存

- **4xx 客户端错误**

    - 400 Bad Request：请求参数错误

    - 401 Unauthorized：未登录 / Token 失效

    - 403 Forbidden：权限不足，拒绝访问

    - 404 Not Found：请求的资源不存在

- **5xx 服务端错误**

    - 500 Internal Server Error：服务器代码异常

    - 503 Service Unavailable：服务不可用、服务过载 / 宕机

    - 504 Gateway Timeout：网关超时

### 2\. 强缓存 vs 协商缓存

|对比项|强缓存|协商缓存|
|---|---|---|
|是否发起网络请求|不发起请求|正常发起请求|
|响应状态码|200 \(from cache\)|304|
|决策方|浏览器本地判断|服务器校验判断|
|性能|速度最快|速度较快|
|核心请求头|`Cache\-Control` / `Expires`|`If\-Modified\-Since` / `If\-None\-Match`|
|运行机制|缓存未过期，直接使用本地资源|缓存过期后，询问服务器资源是否更新，再决定是否使用缓存|

---

## 八、AI 流式方案（SSE \+ HTTP）

### SSE\+HTTP 流式响应 \&amp; Nginx 攒包问题解决

1. **问题现象**
Nginx 默认开启缓冲区，会累积多条数据后批量转发，导致 SSE 无法实现实时流式输出。

2. **解决方案**

    - **Nginx 配置**（关闭缓冲、延长超时）

    ```nginx
    proxy_buffering off;
    proxy_cache off;
    chunked_transfer_encoding on;
    proxy_read_timeout 120s;
    ```

    - **后端响应头**：添加 `X\-Accel\-Buffering: no`，强制关闭 Nginx 缓冲；

    - **后端逻辑**：每条数据生成后主动执行 `flush` 推送，不累积数据。

---

## 九、ECharts 相关

### 1\. ECharts dataset 使用及作用

> 自行补充作答
> 
> 

### 2\. Canvas 和 SVG 渲染模式区别

> 自行补充作答
> 
> 

> （注：文档部分内容可能由 AI 生成）
